name: Build Modpack

on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with: 
          go-version: '1.18'
          check-latest: true

      - name: Download Dependencys
        run: |
          sudo apt install p7zip
          go install github.com/packwiz/packwiz@latest

      - name: Make Modrinth
        run: |
          make modrinth
          7z x ./build/zenith-core-development.mrpack -o./build/.modrinth

      - name: Make Prism Develop
        run: |
          make prism
          7z x ./build/modpack-prism.zip -o./build/.prism

      - name: Make Server
        run: |
          make server

      - name: Upload Modrinth
        uses: actions/upload-artifact@v3
        with:
          name: HexMC Modrinth
          path: build/.modrinth

      - name: Upload Prism (Development)
        uses: actions/upload-artifact@v3
        with:
          name: Zenith-Core Prism Development
          path: build/.prism

      - name: Upload Server
        uses: actions/upload-artifact@v3
        with:
          name: Zenith-Core Server
          path: build/server